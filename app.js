function openImage(imagePath) {
    // Create a modal or lightbox to display the image
    // For simplicity, let's use the browser's built-in alert function
    alert(`Clicked! Opening image: ${imagePath}`);
}
